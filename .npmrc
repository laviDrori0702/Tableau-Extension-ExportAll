# @tableau/tableau-ui@3.2.0 declares a peer of react@^16.8.6 but this project runs
# React 17. It works fine in practice; npm 7+ would otherwise fail the install with
# ERESOLVE — including the `npm install` in render.yaml's build command.
legacy-peer-deps=true
